# Server Configuration
NODE_ENV=development
PORT=3000

# CORS Configuration (comma-separated list of allowed origins)
# In production: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000

# Firebase Admin SDK Credentials
# Get these from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_PROJECT_ID=your-project-id
# IMPORTANT: For the private key, use the ENTIRE key from the JSON file including the header and footer
# The \n characters should be literal backslash-n, not actual newlines
# Example format: "-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
# In production: Copy the entire "private_key" value from the service account JSON (keep the \n as-is)
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the actual .env file to git
# 2. This .env.example file should only contain dummy/example values
# 3. Copy this to .env and fill in real values: cp .env.example .env
# 4. Add .env to .gitignore (already configured)
# 5. For production, set these as environment variables in your deployment platform
